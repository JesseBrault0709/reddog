{
    "name": "new-reddog",
    "version": "0.0.1",
    "description": "",
    "main": "index.js",
    "scripts": {
        "build": "npm run ssg && npm run static && npm run style && npm run prettier",
        "dev": "nodemon",
        "prettier": "prettier -w build",
        "ssg": "ssg/cli/build/install/ssg/bin/ssg --info build",
        "ssgDebug": "ssg/cli/build/install/ssg/bin/ssg --debug build",
        "static": "cp -r images build && cp -r fonts build",
        "style": "sass style:build",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "nodemon": "^2.0.20",
        "prettier": "^2.8.4",
        "sass": "^1.58.3"
    }
}
